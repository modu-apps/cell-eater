{
  "name": "cell-eater",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --watch --serve",
    "test": "npx tsx tests/test-determinism.ts && npx tsx tests/test-rollback.ts",
    "test:determinism": "npx tsx tests/test-determinism.ts",
    "test:rollback": "npx tsx tests/test-rollback.ts",
    "test:input-ordering": "npx tsx tests/test-input-ordering.ts"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0",
    "ws": "^8.0.0"
  },
  "dependencies": {
    "cell-eater": "file:"
  }
}
